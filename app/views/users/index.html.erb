<ol>
<% @users.each do |user|%>
  <% if user.role == "mentor" %>
    <li> <%= link_to "#{user.first_name} #{user.last_name}", mentor_path(user) %></li>
  <% else %>
    <li> <%= link_to "#{user.first_name} #{user.last_name}", student_path(user) %></li>
  <% end %>
<% end %>
</ol>


